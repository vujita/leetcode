// #338 Counting Bits
// Return array ans where ans[i] is the number of 1 bits in i, for 0 <= i <= n.
// Example: n=2 → [0,1,1]  |  n=5 → [0,1,1,2,1,2]

export function countBits(n: number): number[] {
  // TODO
  return [];
}
