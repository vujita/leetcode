// #79 Word Search
// Return true if word exists in the board (sequential adjacent cells, no reuse).
// Example: board=[["A","B","C","E"],...] word="ABCCED" → true

export function exist(board: string[][], word: string): boolean {
  // TODO
  return false;
}
